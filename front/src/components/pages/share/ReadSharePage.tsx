import { useContext } from "react"
import { useNavigate, useParams } from "react-router-dom"

import { UserStateContext } from "../RootPage"
import * as Api from "../../../api"

export default function ReadSharePage({ setIsEditing, shareInfo, setSomethingWasChanged }: { setIsEditing: any; shareInfo: any; setSomethingWasChanged: any }) {
  return <>"굿"</>
}
